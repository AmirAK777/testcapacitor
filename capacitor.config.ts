import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Clovis-Questionnaire',
  webDir: 'dist/clovis-questionnaire/browser'
};

export default config;
